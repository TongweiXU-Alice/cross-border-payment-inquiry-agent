# Optional parser only. Keep disabled for the fully local deterministic demo.
# When enabled, inquiry text is sent to the configured OpenAI API project.
OPENAI_PARSER_ENABLED=0
OPENAI_API_KEY=
OPENAI_MODEL=
